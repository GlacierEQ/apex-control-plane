# Source-controlled Buildkite CI for GlacierEQ/apex-control-plane.
# Mirrors the repository's existing GitHub Actions quality contracts and adds
# durable Buildkite receipts and artifacts.
env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONUNBUFFERED: "1"
  PYTHONDONTWRITEBYTECODE: "1"

steps:
  - label: ":shield: Source and operator-fidelity gate"
    key: source-fidelity
    timeout_in_minutes: 20
    agents:
      queue: macos-self
      os: macos
    retry:
      automatic:
        - exit_status: -1
          limit: 1
        - exit_status: 255
          limit: 1
    command: |
      set -euo pipefail
      mkdir -p artifacts/buildkite
      actual="$(git rev-parse HEAD)"
      test "$$actual" = "$BUILDKITE_COMMIT"
      git diff --exit-code
      python3 -m pip install --upgrade pip
      python3 -m pip install -r requirements.txt pytest
      python3 -m json.tool config/operator_fidelity_runtime_policy.json >/dev/null
      python3 -m json.tool config/apex_connector_contract_registry.json >/dev/null
      python3 -m json.tool config/apex_direct_connector_runtime.json >/dev/null
      python3 -m py_compile \
        src/operator_fidelity_lock.py \
        src/operator_fidelity_preflight.py \
        src/control_plane.py \
        src/direct_connector_runtime_contract.py \
        src/sitecustomize.py
      CASEY_AUTO_BOOT_TESTING=1 PYTHONPATH=src:. python3 -m pytest -q \
        tests/test_operator_fidelity_lock.py \
        tests/test_operator_fidelity_preflight.py \
        tests/test_direct_connector_runtime_contract.py \
        --junitxml=artifacts/buildkite/operator-fidelity.xml \
        2>&1 | tee artifacts/buildkite/operator-fidelity.log
      git diff --exit-code
    artifact_paths:
      - artifacts/buildkite/operator-fidelity.xml
      - artifacts/buildkite/operator-fidelity.log

  - label: ":mag: Registry and audit hardening"
    key: registry-audit
    depends_on: source-fidelity
    timeout_in_minutes: 25
    agents:
      queue: macos-self
      os: macos
    retry:
      automatic:
        - exit_status: -1
          limit: 1
        - exit_status: 255
          limit: 1
    command: |
      set -euo pipefail
      mkdir -p artifacts/buildkite
      python3 -m pip install --upgrade pip
      python3 -m pip install -r requirements.txt ruff mypy pytest
      ruff check \
        scripts/scan_repos.py \
        src/audit_engine.py \
        src/direct_connector_runtime_contract.py \
        apex_runner.py \
        daily_audit.py \
        apex_issue_writer.py \
        tests/test_scan_repos.py \
        tests/test_audit_hardening.py \
        tests/test_direct_connector_runtime_contract.py
      ruff format --check \
        scripts/scan_repos.py \
        src/audit_engine.py \
        src/direct_connector_runtime_contract.py \
        apex_runner.py \
        daily_audit.py \
        apex_issue_writer.py \
        tests/test_scan_repos.py \
        tests/test_audit_hardening.py \
        tests/test_direct_connector_runtime_contract.py
      mypy scripts/scan_repos.py src/direct_connector_runtime_contract.py --ignore-missing-imports
      PYTHONPATH=src:. python3 -m pytest -q \
        tests/test_scan_repos.py \
        tests/test_audit_hardening.py \
        tests/test_direct_connector_runtime_contract.py \
        --junitxml=artifacts/buildkite/registry-audit.xml \
        2>&1 | tee artifacts/buildkite/registry-audit.log
      git diff --exit-code
    artifact_paths:
      - artifacts/buildkite/registry-audit.xml
      - artifacts/buildkite/registry-audit.log

  - label: ":receipt: Verified control-plane CI receipt"
    key: verified-receipt
    depends_on:
      - source-fidelity
      - registry-audit
    timeout_in_minutes: 5
    agents:
      queue: macos-self
      os: macos
    command: |
      set -euo pipefail
      mkdir -p artifacts/buildkite
      actual="$(git rev-parse HEAD)"
      test "$$actual" = "$BUILDKITE_COMMIT"
      sha256sum \
        artifacts/buildkite/operator-fidelity.xml \
        artifacts/buildkite/registry-audit.xml \
        > artifacts/buildkite/SHA256SUMS
      python3 - <<'PY' > artifacts/buildkite/verified-ci-receipt.json
      import datetime
      import json
      import os
      import subprocess
      receipt = {
          "schema": "glaciereq.apex-control-plane.buildkite.v1",
          "status": "PASS",
          "commit": subprocess.check_output(
              ["git", "rev-parse", "HEAD"], text=True
          ).strip(),
          "branch": os.environ.get("BUILDKITE_BRANCH"),
          "build_id": os.environ.get("BUILDKITE_BUILD_ID"),
          "build_number": os.environ.get("BUILDKITE_BUILD_NUMBER"),
          "pipeline": os.environ.get("BUILDKITE_PIPELINE_SLUG"),
          "generated_at": datetime.datetime.now(datetime.timezone.utc)
          .replace(microsecond=0)
          .isoformat(),
      }
      print(json.dumps(receipt, indent=2, sort_keys=True))
      PY
      git diff --exit-code
    artifact_paths:
      - artifacts/buildkite/SHA256SUMS
      - artifacts/buildkite/verified-ci-receipt.json
