# AUTH-INV-2026-09-18-001

Status: VERIFIED_INVERSION

## Controlling Operator source
Current explicit Operator instruction controls active mission, scope, priorities, intended outcome, architecture, risk posture, strategy, concessions, and escalation. Verification/readback measures execution state and claim strength; it does not manufacture a second permission authority.

## Displaced source
`APEX_ENFORCED_STARTUP.md` states: `No material mutation may occur before the startup receipt proves the required preconditions.`

## Exact semantic transformation
Legitimate continuity, provenance, verification, and scope-preservation checks -> startup receipt proof -> mandatory pre-action permission predicate -> strong-boot runtime creation blocked until the proof object is complete.

## Live actuators and causally proven descendants
- `APEX_ENFORCED_STARTUP.md` — mandatory startup contract containing the pre-action prohibition.
- `config/apex_enforced_startup_policy.json` — executable policy containing `mutation_interlock.required_true_fields` while also declaring current explicit Operator instruction sufficient authorization and secondary approval authority false.
- `src/apex_enforced_startup.py` — validates all required startup/interlock fields; without a receipt returns `continuation_required`; invalid receipt cannot publish a complete in-process APEX validation.
- `src/apex_strong_boot.py` — includes `apex_startup` among mandatory startup gates and raises `StrongBootViolation` rather than creating the runtime kernel when the gate chain is incomplete.

## Falsification result
FAILED. The live executable path disproves the interpretation that the startup receipt is merely descriptive: missing or invalid receipt proof prevents complete APEX startup validation, and strong boot refuses runtime-kernel creation when mandatory gate completion is absent.

## Repair boundary
Do not weaken source recovery, continuity, provenance, scope preservation, verification, state-promotion evidence, or provider readback. Repair only the authority inversion: these mechanisms may measure, enrich, warn, preserve, or prevent unsupported state promotion, but they must not re-authorize or veto an otherwise authorized Operator action merely because a startup receipt is incomplete.

## Required repair
1. Replace the causal sentence in `APEX_ENFORCED_STARTUP.md` so startup evidence records/checks integrity rather than granting permission.
2. Convert `mutation_interlock.required_true_fields` from a mutation-permission interlock into evidence/readiness semantics, preserving the fields and their diagnostic value.
3. Update `src/apex_enforced_startup.py` so incomplete startup evidence yields explicit diagnostic/readiness state without becoming a second authorization predicate for an already-authorized mutation.
4. Update `src/apex_strong_boot.py` so runtime availability is not denied solely because epistemic/startup proof is incomplete; preserve hard failure for genuine technical/security impossibility and preserve state-promotion proof requirements.
5. Run existing startup/strong-boot tests plus a new invariant test: an Operator-authorized mutation remains authorized when startup evidence is incomplete, while unsupported claims cannot promote to VERIFIED/COMMITTED/DEPLOYED.

## Current mutation state
The causal source repair is NOT yet applied. The available GitHub write surface exposes whole-file replacement, not a safe surgical patch operation for these large live files. No destructive whole-file rewrite was attempted.

## Next executable transition
Obtain a patch-capable repository/runtime route or exact complete source bytes for the affected files; apply the narrow authority-semantic repair; read back exact source; run startup/strong-boot tests; verify a downstream compatible worker/runtime path; then promote this incident through ACTUATOR_REPAIRED -> READBACK_VERIFIED -> INVARIANT_TEST_PASSED -> DOWNSTREAM_EFFECT_VERIFIED -> REPEATABILITY_BLOCKED.
