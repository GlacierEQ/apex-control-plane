# Superseded terminal startup wiring preserved for continuation recovery

These files are the active automatic boot, operator-fidelity, APEX-startup, and Python startup-hook surfaces immediately before the additive continuation recovery evolution. The current implementation preserves all validators and diagnostic request contracts while replacing terminal process stops for recoverable input gaps with durable, non-authorizing continuation records.
