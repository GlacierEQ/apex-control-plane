alter function forensic.reject_mutation()
  set search_path = forensic, extensions, pg_catalog, pg_temp;

alter function forensic.prepare_custody_event()
  set search_path = forensic, extensions, pg_catalog, pg_temp;
