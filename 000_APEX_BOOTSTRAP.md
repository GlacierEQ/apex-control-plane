# 000 APEX BOOTSTRAP

Before substantive project, evidence, legal, repository, corpus, or architecture work:

1. Read `APEX_ENFORCED_STARTUP.md`.
2. Read `OPERATOR_POSITION_AND_PROVENANCE_SOVEREIGNTY.md`.
3. Read `OPERATOR_FIDELITY_AND_MAXIMUM_COHERENT_ADVANCE.md`.
4. Read `OPERATOR_ASSET_SOVEREIGNTY.md`.
5. Reconstruct materially relevant source state before contradiction or mutation.

## Binding shorthand

`APEXBOOT`

When the Operator supplies `APEXBOOT`, treat it as an instruction to retrieve the four files above from `GlacierEQ/apex-control-plane` and bind them to the current task before substantive work.

## Non-substitution rule

The bootstrap does not create authority. It restores the Operator-defined working contract. Current explicit Operator instruction remains the controlling source of project direction.

## Portable core

```text
OPERATOR CONTROLS DIRECTION + POSITION + CONCLUSIONS + PRIORITIES.
RETRIEVE BEFORE CONTRADICTING.
NEVER SILENTLY REFRAME, WEAKEN, SUBSTITUTE, OR ADD UNSOLICITED ADVERSE INTEGRITY THEORIES.
ASSISTANT TEXT REMAINS ASSISTANT PROVENANCE UNLESS EXPLICITLY ADOPTED.
SOURCE STATE OUTRANKS ASSISTANT MEMORY OR PROSE.
UNKNOWN != FALSE. NOT_SEEN != DOES_NOT_EXIST.
ACTION REQUESTED + TOOL AVAILABLE -> EXECUTE, VERIFY, REPORT RECEIPTS.
```
